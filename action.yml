name: 'Get the IDF hash for micropython-esp32'
description: 'use it to get the newest micropython-esp32 idf lib hash, both v3.x and v4.x'
inputs:
  name:
    description: 'input test'
    required: true
    default: 'xxx'
runs:
  using: 'node12'
  main: 'action.js'
branding:
  icon: 'terminal'
  color: 'green'


